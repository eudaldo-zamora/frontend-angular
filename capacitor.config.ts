import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ec.edu.utm.frontend',
  appName: 'frontend',
  webDir: 'dist/frontend',
  bundledWebRuntime: false
};

export default config;
